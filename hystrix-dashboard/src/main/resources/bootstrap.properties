spring.application.name=hystrix-dashboard

# This information need to happen before the rest of the application's configuration is evaluated,
# i.e. it needs to happen in the bootstrap.
spring.cloud.config.uri=http://localhost:8888
